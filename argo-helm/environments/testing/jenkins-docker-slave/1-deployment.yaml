apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker-slave
  namespace: docker-testing
  labels:
    app: docker-testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docker-testing
  template:
    metadata:
      labels:
        app: docker-testing
    spec:
      containers:
        - name: docker-testing
          image: slobodang/jenkins-docker-slave:latest
          ports:
            - containerPort: 22
