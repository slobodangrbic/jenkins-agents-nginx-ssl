apiVersion: apps/v1
kind: Deployment
metadata:
  name: postman-master
  namespace: postman-staging
  labels:
    app: postman-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postman-staging
  template:
    metadata:
      labels:
        app: postman-staging
    spec:
      containers:
        - name: postman-staging
          image: slobodang/jenkins-postman-slave:latest
          ports:
            - containerPort: 22
