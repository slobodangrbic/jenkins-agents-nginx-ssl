apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-master
  namespace: jenkins-dev
  labels:
    app: jenkins-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-dev
  template:
    metadata:
      labels:
        app: jenkins-dev
    spec:
      containers:
        - name: jenkins-dev
          image: slobodang/jenkins-master:latest
          ports:
            - containerPort: 8080
