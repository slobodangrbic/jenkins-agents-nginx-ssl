---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: docker-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/slobodangrbic/jenkins-agents-nginx-ssl.git
    targetRevision: dev
    path: argo-helm/environments/dev/jenkins-docker-slave
  destination:
    server: 'https://kubernetes.default.svc'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true 
      allowEmpty: true
    syncOptions:
    - CreateNamespace=true
    - Validate=true
    - PruneLast=true
    - PrunePropagationPolicy=Foreground